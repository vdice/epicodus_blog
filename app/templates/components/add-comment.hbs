<h1>Add Comment</h1>
<div class="container">
  <form class="form-horizontal">
    <div class="form-group">
      <label for="comment">Comment</label>
      {{textarea row="4" cols="50" value=comment id="comment" class="form-control"}}
    </div>

    <div class="form-group">
      <label for="user">User</label>
      {{input value=user id="user" class="form-control"}}
    </div>

    <button class="btn btn-primary" {{action 'addComment' post}}>Save</button>
  </form>
</div>
